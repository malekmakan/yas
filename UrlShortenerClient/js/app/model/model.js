(function () {
    'use strict';

    define([
            'model/model.modelUrl'
        ],
        function (modelUrl) {

            return {
                ModelUrl: modelUrl
            };
        });
})();